import { EquipImages } from '../UI/equip'
import { images } from '../UI/inventory'
import { tooltip } from '../UI/tooltip'

export const IMAGE = {
  WON: `./images/money/won.png`,
  STAR_FORCE: `./images/star-force/star.png`,
  MUSHROOM: `./images/stand.gif`
}

export default {
  EquipImages,
  images,
  tooltip
}
